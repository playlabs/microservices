spring:
    profiles: prod
    datasource:
        url: jdbc:mysql://localhost:3306/sakila
        username: root
        password: password
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        show-sql: true  
server:
  port: 8081  
rabbitmq:
   addresses: 10.0.0.35:5671
   host: configmq
   port: 5671
   username: guest
   password: guest